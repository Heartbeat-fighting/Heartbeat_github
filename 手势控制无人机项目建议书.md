工程学导论项目
B第二组  陈胤成 徐靖 魏铭宏 张益玮

# 基于STM32的手势控制无人机系统

## 项目建议书

---

## 项目摘要

传统无人机依赖遥控器操作，学习成本高、交互方式不够直观。本项目基于STM32微控制器与MPU6050 IMU传感器，开发一套手势识别控制无人机系统。用户佩戴手势手套，通过手腕姿态与动作直接控制无人机飞行，实现更自然、低门槛的人机交互方式。系统涵盖嵌入式姿态解算、手势识别算法、2.4GHz无线通信及飞控集成四个核心技术模块，具备较高的学术价值与应用前景。

---

## 目录

1. 需求论证与市场分析
2. 产品总体介绍
3. 系统设计方案
4. 核心代码实现
5. 研发难点与解决方案
6. 可行性论证
7. 项目团队与计划进度
8. 竞争对手分析
9. 风险与收益

---

## 一、需求论证与市场分析

### 1.1 问题陈述

当前消费级无人机市场高度依赖物理遥控器，存在以下痛点：

- **学习门槛高**：双摇杆操控需要大量练习，新手难以快速上手
- **交互不直觉**：摇杆位移与飞机运动之间缺乏自然映射关系
- **场景受限**：遥控器需双手持握，限制了使用场景的灵活性

手势控制作为一种更自然的交互方式，能够将人体动作直接映射为飞行指令，显著降低操控门槛。

### 1.2 用户需求分析

目标用户群体及其核心需求如下：

| 用户群体 | 核心需求 | 痛点 |
|---------|---------|------|
| 无人机初学者 | 快速上手，直觉操控 | 遥控器学习曲线陡峭 |
| 教育/展示场景用户 | 互动性强，演示效果好 | 传统遥控器缺乏表现力 |
| 竞技/创客爱好者 | 新颖的控制方式，可扩展 | 现有方案功能单一 |
| 科研人员 | 人机交互研究平台 | 缺乏低成本开放平台 |

### 1.3 市场分析

#### 1.3.1 市场规模

全球消费级无人机市场持续增长，2023年市场规模约为134亿美元，预计2028年达到430亿美元（CAGR约26%）。其中教育、娱乐及创客领域增速最快，对新型交互方式的需求旺盛。

#### 1.3.2 技术趋势

- 手势识别技术逐步从手机端向嵌入式端迁移
- IMU传感器成本持续下降，精度不断提升
- 开源飞控生态（Betaflight/PX4）成熟，降低了二次开发门槛
- 低功耗无线通信模块（nRF24L01+）性价比高，适合原型开发


---

## 二、产品总体介绍

### 2.1 项目目标

开发一套完整的手势控制无人机系统，实现：

- 手势端实时采集手腕姿态数据，识别12种飞行控制手势
- 通过2.4GHz无线链路将指令低延迟传输至无人机端
- 无人机端解析指令并执行对应飞行动作
- 完整的安全保护机制（信号丢失自动降落、紧急停机等）

### 2.2 系统架构

系统分为两个独立子系统，通过无线链路连接：

```
┌─────────────────────────┐         ┌─────────────────────────┐
│       手势控制端         │  2.4GHz │       无人机端           │
│                         │?───────?│                         │
│  MPU6050 → STM32F103   │         │  nRF24L01+ → 飞控板     │
│  姿态解算 → 手势识别    │         │  指令解析 → 电机控制    │
│  OLED显示 + 按键        │         │  安全监控 + PID调节     │
└─────────────────────────┘         └─────────────────────────┘
```

### 2.3 技术创新点

1. **融合滤波姿态解算**：将互补滤波与卡尔曼滤波结合，在低算力MCU上实现高精度姿态估计
2. **双模手势识别**：静态角度阈值判断与动态加速度事件检测相结合，覆盖更多手势类型
3. **自定义轻量通信协议**：针对实时控制场景优化的数据包结构，端到端延迟 < 50ms
4. **分层安全架构**：手势端、通信层、飞控端三层独立安全保护

---

## 三、系统设计方案

### 3.1 手势端设计

#### 3.1.1 硬件架构

```
MPU6050 (I2C) ──? STM32F103C8T6 ──? nRF24L01+ (SPI)
                       │
                  OLED显示屏 (I2C)
                  振动反馈电机
                  功能按键 ×3
```

#### 3.1.2 软件模块划分

```
GestureGlove/
├── Core/
│   ├── imu_sensor.c/h        // MPU6050驱动与数据采集
│   ├── attitude_estimator.c/h // 姿态解算（互补滤波）
│   ├── gesture_recognizer.c/h // 手势识别算法
│   ├── wireless_comm.c/h      // nRF24L01+通信驱动
│   └── display.c/h            // OLED显示控制
└── main.c                     // 主循环调度
```

#### 3.1.3 主循环逻辑

系统以100Hz频率运行主循环，各模块按优先级顺序执行：

```c
// 主循环 - 100Hz
while(1) {
    // 1. 采集IMU原始数据
    imu_read_raw(&accel, &gyro);

    // 2. 姿态解算（互补滤波）
    attitude_update(&accel, &gyro, dt);
    attitude_get_euler(&pitch, &roll, &yaw);

    // 3. 手势识别
    gesture = gesture_recognize(pitch, roll, yaw, &accel);

    // 4. 生成并发送飞行指令
    if (gesture != GESTURE_NONE) {
        cmd = gesture_to_command(gesture);
        wireless_send(&cmd, sizeof(cmd));
    }

    // 5. 更新显示
    display_update(pitch, roll, gesture);

    HAL_Delay(10); // 10ms周期
}
```

### 3.2 无人机端设计

#### 3.2.1 软件架构

```
DroneReceiver/
├── nrf24_receiver.c/h     // 无线接收驱动
├── command_parser.c/h     // 指令解析与映射
├── betaflight_bridge.c/h  // MSP协议与飞控通信
├── safety_monitor.c/h     // 安全监控模块
└── main.c
```

#### 3.2.2 指令映射表

| 手势动作 | 触发条件 | 飞行指令 | 控制范围 |
|---------|---------|---------|---------|
| 手腕前倾 | Pitch > 15° | 前飞 (Pitch+) | 15°~45° |
| 手腕后仰 | Pitch < -15° | 后退 (Pitch-) | -45°~-15° |
| 手腕左倾 | Roll < -15° | 左移 (Roll-) | -45°~-15° |
| 手腕右倾 | Roll > 15° | 右移 (Roll+) | 15°~45° |
| 腕部左旋 | Yaw变化 < -10°/s | 左转 (Yaw-) | 连续 |
| 腕部右旋 | Yaw变化 > 10°/s | 右转 (Yaw+) | 连续 |
| 快速上抬 | 加速度 > 2g | 起飞/上升 | — |
| 快速下压 | 加速度 < -2g | 降落/下降 | — |
| 握拳保持3s | 加速度变化 < 0.1g | 紧急停机 | — |

### 3.3 通信协议设计

自定义轻量级数据包，最小化传输延迟：

```c
// 飞行指令数据包结构（12字节）
typedef struct {
    uint8_t  header;      // 帧头 0xAA
    uint8_t  cmd_type;    // 指令类型
    int16_t  pitch;       // 俯仰角 × 100（定点数）
    int16_t  roll;        // 横滚角 × 100
    int16_t  yaw_rate;    // 偏航角速度 × 10
    uint8_t  throttle;    // 油门 0~100
    uint8_t  flags;       // 状态标志位
    uint8_t  checksum;    // XOR校验
    uint8_t  footer;      // 帧尾 0x55
} FlightCommand_t;

// 指令类型定义
#define CMD_IDLE           0x00
#define CMD_ARM            0x01
#define CMD_DISARM         0x02
#define CMD_TAKEOFF        0x03
#define CMD_LAND           0x04
#define CMD_FLIGHT_CTRL    0x05
#define CMD_EMERGENCY_STOP 0xFF
```


---

## 四、核心代码实现

### 4.1 姿态解算——互补滤波

MPU6050同时提供加速度计和陀螺仪数据，两者各有缺陷：加速度计短期噪声大但长期稳定，陀螺仪短期精确但长期积分漂移。互补滤波将两者融合：

```c
// 互补滤波姿态解算
// alpha = 0.98：高通滤波陀螺仪，低通滤波加速度计
#define ALPHA 0.98f

void attitude_update(Vector3_t *accel, Vector3_t *gyro, float dt) {
    // 由加速度计计算倾角（低频参考）
    float accel_pitch = atan2f(accel->y,
                        sqrtf(accel->x * accel->x + accel->z * accel->z));
    float accel_roll  = atan2f(-accel->x, accel->z);

    // 陀螺仪积分（高频跟踪）
    g_attitude.pitch += gyro->x * dt;
    g_attitude.roll  += gyro->y * dt;

    // 互补融合
    g_attitude.pitch = ALPHA * g_attitude.pitch + (1.0f - ALPHA) * accel_pitch;
    g_attitude.roll  = ALPHA * g_attitude.roll  + (1.0f - ALPHA) * accel_roll;

    // 偏航角仅用陀螺仪积分（无磁力计时无绝对参考）
    g_attitude.yaw += gyro->z * dt;
}
```

### 4.2 手势识别——双模融合

```c
GestureType_t gesture_recognize(EulerAngles_t *angles, Vector3_t *accel) {

    // --- 动态手势：基于加速度突变事件 ---
    float accel_mag = sqrtf(accel->x*accel->x +
                            accel->y*accel->y +
                            accel->z*accel->z);
    if (accel_mag > TAKEOFF_ACCEL_THRESHOLD) {  // > 2g
        return gesture_confirm(GESTURE_TAKEOFF);
    }
    if (accel_mag < LAND_ACCEL_THRESHOLD) {     // < -2g（相对重力）
        return gesture_confirm(GESTURE_LAND);
    }

    // --- 静态手势：基于姿态角阈值 ---
    if (angles->pitch > PITCH_FWD_MIN)  return GESTURE_FORWARD;
    if (angles->pitch < PITCH_BWD_MAX)  return GESTURE_BACKWARD;
    if (angles->roll  < ROLL_LEFT_MAX)  return GESTURE_LEFT;
    if (angles->roll  > ROLL_RIGHT_MIN) return GESTURE_RIGHT;

    // --- 悬停：握拳检测（加速度变化极小）---
    if (accel_variance() < HOLD_VARIANCE_THRESHOLD) {
        hold_timer += dt;
        if (hold_timer > 3.0f) return GESTURE_EMERGENCY_STOP;
        return GESTURE_HOLD;
    } else {
        hold_timer = 0;
    }

    return GESTURE_NONE;
}

// 确认机制：连续N帧一致才触发，防止误触
static GestureType_t gesture_confirm(GestureType_t candidate) {
    if (candidate == last_gesture) {
        confirm_count++;
        if (confirm_count >= CONFIRM_FRAMES) { // 3帧 = 30ms
            confirm_count = 0;
            return candidate;
        }
    } else {
        last_gesture = candidate;
        confirm_count = 1;
    }
    return GESTURE_NONE;
}
```

### 4.3 安全监控模块

```c
// 三层安全保护，任一触发则执行紧急处理
void safety_monitor_tick(void) {

    // 层1：信号丢失检测
    if (HAL_GetTick() - last_signal_tick > SIGNAL_TIMEOUT_MS) { // 500ms
        signal_lost_count++;
        if (signal_lost_count > 4) {  // 2秒无信号
            fc_emergency_land();
            return;
        }
        fc_hover();  // 先悬停等待
        return;
    }
    signal_lost_count = 0;

    // 层2：电池电量监控
    float battery_pct = battery_get_percentage();
    if (battery_pct < 20.0f) {
        fc_force_land();
        return;
    }
    if (battery_pct < 30.0f) {
        display_warn("LOW BATTERY");
    }

    // 层3：飞行边界限制
    if (fabsf(fc_get_roll()) > MAX_TILT_ANGLE ||
        fabsf(fc_get_pitch()) > MAX_TILT_ANGLE) {
        fc_level_recovery();  // 强制回平
    }
}
```

---

## 五、研发难点与解决方案

### 5.1 姿态解算精度问题

**难点**：MPU6050陀螺仪存在温漂，长时间使用后姿态角累积误差明显，导致手势识别基准偏移。

**解决方案**：
- 采用互补滤波融合加速度计与陀螺仪，系数α=0.98，有效抑制低频漂移
- 上电时执行静态校准，采集200帧数据取均值作为零偏补偿
- 提供用户触发的实时重校准功能（长按按键3秒）
- 后期可升级为卡尔曼滤波，进一步提升动态精度

### 5.2 手势误触发问题

**难点**：手部自然抖动、过渡动作容易被误识别为控制指令，导致无人机行为异常。

**解决方案**：
- 引入**时间窗口确认机制**：同一手势需连续3帧（30ms）一致才触发
- 设置**信号量死区**：角度阈值附近设置±3°缓冲区，避免边界抖动
- 实现**互斥逻辑**：同一时刻只允许识别一种手势，优先级由高到低排列
- 动态手势（起飞/降落）采用加速度幅值而非方向，降低方向误判概率

### 5.3 无线通信延迟与稳定性

**难点**：2.4GHz频段干扰严重（WiFi、蓝牙共存），实测延迟波动大，偶发丢包导致无人机控制不连续。

**解决方案**：
- 使用nRF24L01+ PA+LNA增强版模块，提升信号强度与抗干扰能力
- 实现**自动重传机制（ARQ）**：关键指令（起飞/停机）最多重传5次
- 采用**信道扫描**：启动时自动选择干扰最小的频道（76个可选信道）
- 优化数据包大小至12字节，降低单次传输时间，提升有效帧率
- 加入CRC校验，丢弃损坏数据包，避免错误指令执行

### 5.4 飞行稳定性调试

**难点**：手势指令为离散事件，而飞控需要连续平滑的PWM输入，直接映射导致飞行抖动明显。

**解决方案**：
- 在无人机端实现**指令平滑插值**：新指令到来时以斜坡方式过渡，而非阶跃
- 使用Betaflight的Angle模式（自稳模式），飞控自动维持水平，降低对手势精度的要求
- 限制最大倾斜角度为30°，防止激进手势导致失控
- 通过Betaflight Configurator反复调整PID参数，优化对手势指令的响应速度与稳定性

### 5.5 系统集成调试

**难点**：手势端与无人机端独立开发，联调时发现通信协议字节序、指令映射方向等存在不一致。

**解决方案**：
- 制定统一的通信协议文档，明确每个字段的数据类型、范围和方向定义
- 先用串口打印验证手势端输出，再接入无人机端，分阶段排查问题
- 使用Betaflight Configurator的RC通道监视功能，实时验证指令是否正确到达飞控


---

## 六、竞争对手分析

### 6.1 竞品对比

| 产品/方案 | 控制方式 | 技术路线 | 价格 | 开放性 | 主要不足 |
|---------|---------|---------|------|-------|---------|
| DJI Spark（手势模式） | 视觉手势 | 图像识别 | 2999元 | 封闭 | 需光线充足，延迟高，手势种类少 |
| Leap Motion + 无人机 | 光学手势 | 红外深度 | 1500元+ | 半开放 | 依赖PC，使用场景受限 |
| 传统遥控器方案 | 双摇杆 | PWM/SBUS | 200~500元 | 开放 | 学习成本高，不直觉 |
| 肌电手环方案 | 肌电信号 | EMG识别 | 3000元+ | 封闭 | 成本极高，个体差异大 |
| **本项目** | IMU手势 | 姿态识别 | ~800元 | 完全开放 | — |

### 6.2 SWOT分析

**优势（Strengths）**
- 成本远低于视觉/肌电方案，800元以内完成完整系统
- 完全开源，代码与硬件均可二次开发
- 不依赖光线、环境，室内外均可使用
- 延迟低（<50ms），响应优于视觉识别方案

**劣势（Weaknesses）**
- 手势种类受IMU限制，无法识别手指动作
- 需要佩戴手套设备，使用便利性低于纯视觉方案
- 无GPS，悬停精度依赖飞控自稳，存在漂移

**机会（Opportunities）**
- 教育机器人市场对低成本开放平台需求旺盛
- 创客/竞赛场景对新型交互方式接受度高
- IMU传感器精度持续提升，识别算法可不断优化

**威胁（Threats）**
- DJI等大厂持续完善手势控制功能
- 视觉AI算法快速发展，可能使IMU方案竞争力下降

### 6.3 差异化定位

本项目定位为**低成本、开源、可二次开发的手势控制无人机研究平台**，核心竞争力不在于与商业产品比拼功能完整度，而在于：
- 提供完整可复现的技术实现路径
- 适合教学演示与学术研究
- 为后续扩展（视觉融合、机器学习手势识别）提供基础框架

---

## 七、项目团队与计划进度

### 7.1 团队分工

| 成员 | 职责 |
|------|------|
| 成员A | 无人机结构设计、硬件组装 |
| 成员B | 硬件电路连接、飞控调试 |
| 成员C | 飞控固件配置、无人机端接收程序开发 |
| 成员D | 系统集成测试、飞行测试、安全规范 |
| 手势端团队 | STM32程序开发、姿态解算、手势识别算法 |

### 7.2 计划进度

| 周次 | 主要任务 | 关键里程碑 |
|------|---------|-----------|
| 第1周 | 环境搭建、器件采购、文档学习 | 开发环境就绪 |
| 第2周 | 手势端基础驱动开发（IMU读取、OLED显示） | 姿态数据实时显示 |
| 第3周 | 手势识别算法实现与测试 | 12种手势识别准确率>90% |
| 第4周 | 无人机组装、飞控配置、接收端开发 | 无人机可稳定悬停 |
| 第5周 | 系统联调、通信测试、Gazebo仿真验证 | 端到端延迟<50ms |
| 第6周 | 实飞测试、安全验证、优化与演示准备 | 完整飞行演示 |

### 7.3 考核指标

- 手势识别准确率 > 95%
- 端到端通信延迟 < 50ms
- 信号丢失恢复时间 < 1s
- 支持手势种类 ≥ 8种
- 飞行时间 ≥ 5分钟

---

## 八、风险与收益

### 8.1 主要风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| 手势识别准确率不足 | 中 | 高 | 增加训练数据、优化阈值、引入确认机制 |
| 通信不稳定导致失控 | 中 | 高 | 多层安全保护、信号丢失自动降落 |
| 飞行测试中炸机 | 低 | 中 | 系绳测试、低空验证、安装保护框 |
| 进度延误 | 中 | 中 | 模块化开发，手势端与无人机端并行推进 |

### 8.2 预期收益

**学术价值**
- 完整实践嵌入式系统开发、传感器融合、无线通信、飞控集成等多个技术领域
- 形成可复用的手势识别算法库与通信协议框架

**应用价值**
- 可扩展为无人机编队控制、VR/AR交互、工业遥操作等场景
- 开源后可作为高校教学与竞赛的参考平台

**展示价值**
- 手势控制飞行演示效果直观，适合课程答辩与科技展示

---

## 九、参考资料

1. STMicroelectronics. *STM32F103xx Reference Manual*. 2023.
2. InvenSense. *MPU-6000/6050 Product Specification*. 2013.
3. Nordic Semiconductor. *nRF24L01+ Product Specification*. 2008.
4. Betaflight. *Betaflight Configurator Documentation*. 2024.
5. Zhang, Y., et al. "Gesture-Based Control of Quadcopter Using IMU Sensors." *IEEE Transactions on Industrial Electronics*, 2020.
6. Mahony, R., et al. "Nonlinear Complementary Filters on the Special Orthogonal Group." *IEEE Transactions on Automatic Control*, 2008.

---

*文档版本：v1.0 | 日期：2026年3月*
